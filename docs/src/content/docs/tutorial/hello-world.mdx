---
title: Hello, world!
---

import { Code } from "@astrojs/starlight/components";
import { Aside } from "@astrojs/starlight/components";

We will create our first block: a simple "Hello!" block that will return the string "World!"

## Creating a block

Clippy blocks are EMCAScript modules. The `def` property is where you put your block's JavaScript code, and all other
properties are used by TurboWarp.

Create a file called `src/blocks/hello.js` with this code:

```js
export default {
  title: "Hello!",
  blockType: Scratch.BlockType.REPORTER,
  def() {
    return "World!";
  },
};
```

<Aside type="danger">
**NEVER rename or delete a block file after adding that block!**
Clippy uses your block's filename as the block opcode. As a result, if you rename the filename, you will **BREAK** existing
projects.

If you want to remove a block, move it to `src/blocks/hidden` - it will still exist but will not show in the block palette.

</Aside>

We recommend snake_case or camelCase for all block filenames.
For example: if your block is called "turn around", you might call its definition `turnAround.js` or `turn_around.js`.

## Try your extension

Run `clippy dev` and go to http://localhost:8000 to open the TurboWarp editor with your extension automatically loaded.

<Aside>
If you want your extension to target another Scratch Mod such as [AmpMod](https://ampmod.codeberg.page), specify `-m [mod ID]`:

- `tw`: TurboWarp (this is the default)
- `amp`: AmpMod
- `pm`
  </Aside>
